FROM conda/miniconda3

ENV NB_USER attendee
ENV NB_UID 1000
ENV HOME /home/${NB_USER}

RUN adduser --disabled-password \
    --gecos "Default user" \
    --uid ${NB_UID} \
    ${NB_USER}

COPY . ${HOME}
USER root
RUN chown -R ${NB_UID} ${HOME}

USER ${NB_USER}
WORKDIR ${HOME}
RUN /bin/bash -c "source binder/bootstrap_venv.sh"

CMD ["/bin/bash", "-c", \
     "source activate inns-2019 && jupyter lab --port=8888 --ip=0.0.0.0"]
